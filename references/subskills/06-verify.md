# Subskill: Verify Repository

Prefer a fresh independent context.

Review final diff against baseline and cleanup plan.

Check for behavior-sensitive edits, contract/config/schema/dependency changes, broken references, and lost rationale.

Run repository-native validation when feasible.

Return `verified`, `failed`, or `incomplete` with evidence and gaps.
