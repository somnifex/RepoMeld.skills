# 2026-09-24

## RepoMeld 公众号推文

- 用户要求：为 RepoMeld（收敛 AI/SDD 开发痕迹的仓库卫生 Skill）写一篇公众号推文，**自家 Skill 推广，但读者是技术人员，标题与内容不要夸张**。
- 资料搜集渠道（可复用）：搜狗微信搜索 `https://weixin.sogou.com/weixin?type=2&query=<关键词>` 能拿到真实公众号文章（标题/号名/日期/摘要），比通用搜索引擎好用。
- 关键素材：OpenAI《Harness engineering》原文——每周五 20% 工程时间人工清理 AI slop，"didn't scale"，改为 golden principles + 后台 garbage collection agent 开定向重构 PR。
- 主要竞品：Sentry `deslop`（diff-scoped，prompt-only）、`agent-sh/deslop`（HIGH/MEDIUM/LOW 确定性分档）、OmX `ai-slop-cleaner`（七类 Smell + 四轮 Pass）；相邻赛道 knip/ts-prune/vulture/jscpd/madge。
- 差异化卡位：全仓语义切分 + 双 barrier + L0–L5 风险分级 + 知识归一化（非删除）+ 注释删改补 + 未完成任务默认 retain + 独立 Verifier + runtime-agnostic。

- 第二版按用户给出的中文语体规范逐句改写（去比喻动词、去"不是X而是Y"与"本质上"、去破折号与箭头、去提示词与程度副词、结尾不升华）。规范已存入 `~/.workbuddy-ai/MEMORY.md` 供后续复用。

- 第三版产出公众号可用的排版文件 `.wechat/RepoMeld-公众号排版.html`：全内联样式（公众号会过滤 `<style>` 与 class），`#article` 为复制区，页面顶部两个按钮用 `Range + execCommand('copy')` 复制富文本。标题定为「RepoMeld：一个用于整理仓库 AI/SDD 开发痕迹的 Skill」，开源地址 https://github.com/somnifex/RepoMeld.skills，MIT。

- 第四版按更细的语体规范再优化：删掉「代表性的有三个 / 使用建议有三条 / 下面是设计上的几个选择」这类先报数与过渡旁白，删掉重复的导航句，被动句改主动（`被设为`→`OmX 把它设为`，`在报告里公开`→`写进报告`），互动式收尾改平实（开 Issue 那句）。两份文件（md 与 html）已同步。

- 第五版：派 3 个 subagent 并行审查（成篇章法 / 句法语体 / 事实一致性），主对话汇总改写。查出一条硬伤——那段加引号的 OpenAI 英文「原文」实为 engineering.fyi 的二手转述，非逐字原文（原文是 "every Friday (20% of the week)"），已改为译出大意并交代出处；另修正 bundle 含两个脚本而非一个、补 OpenCodeAdapter、barrier 行允许修改改回「否」、补 stale generated notes。标题去掉「X：一个用于Y的Z」模板，改为「RepoMeld：整理仓库里的 AI/SDD 开发痕迹」。对仗警句全部删除、清单压到三项加「等」、第五节否定连排改正面边界表述、结尾不再复述开头论点。
- 遗留：第五节与第六节缺一手内容（真实运行的数字、被推翻过的默认策略），已写进 md 元信息块建议作者补。

## 项目约定

- 未发布草稿（如公众号推文）放在 `.wechat/`，该目录已在根 `.gitignore` 中忽略；目录内另有一份自保护 `.gitignore`（`*` + `!.gitignore`）。
